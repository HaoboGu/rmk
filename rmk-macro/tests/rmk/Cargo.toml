[package]
name = "rmk"
version = "0.8.2"
edition = "2024"

[dependencies]
futures = { version = "0.3", default-features = false, features = ["async-await"] }

[dev-dependencies]
rmk-macro = { path = "../.." }
