[package]
name = "rmk-rp2040"
version = "0.2.0"
authors = ["Haobo Gu <haobogu@outlook.com>"]
description = "Keyboard firmware written in Rust"
homepage = "https://github.com/haobogu/rmk"
repository = "https://github.com/haobogu/rmk"
readme = "../../README.md"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
rmk = { path = "../../../rmk", features = ["split", "rp2040"] }
embassy-time = { version = "0.5", features = ["defmt"] }
embassy-rp = { version = "0.9", features = [
    "rp2040",
    "defmt",
    "time-driver",
    "critical-section-impl",
] }
embassy-executor = { version = "0.9", features = [
    "defmt",
    "arch-cortex-m",
    "executor-thread",
] }
embassy-futures = { version = "0.1", features = ["defmt"] }
cortex-m-rt = "0.7.5"
portable-atomic = { version = "1.11", features = ["critical-section"] }
defmt = "1.0"
defmt-rtt = "1.0"
panic-probe = { version = "1.0", features = ["print-defmt"] }
static_cell = "2"


# [features]
# avoid having to use --allow-multiple-definition linker flag
# on macOS with Apple Silicon at least
# default = ["rp-pico/disable-intrinsics"]

[build-dependencies]
xz2 = "0.1.7"
json = "0.12"
const-gen = "1.6"

# Split keyboard example
[[bin]]
name = "central"
path = "src/central.rs"

[[bin]]
name = "peripheral"
path = "src/peripheral.rs"

[profile.dev]
codegen-units = 1      # better optimizations
debug = true
opt-level = 1
overflow-checks = true
lto = false
panic = 'unwind'

[profile.release]
codegen-units = 1       # better optimizations
debug = true            # no overhead for bare-metal
opt-level = "z"         # optimize for binary size
overflow-checks = false
lto = "fat"

[patch.crates-io]
# embassy-time-driver ={ git = "https://github.com/embassy-rs/embassy", rev = "85606cc658695230c0d89ae6b815649ab9a9307e" }
embassy-rp ={ git = "https://github.com/embassy-rs/embassy", rev = "85606cc658695230c0d89ae6b815649ab9a9307e" }
# embassy-sync ={ git = "https://github.com/embassy-rs/embassy", rev = "85606cc658695230c0d89ae6b815649ab9a9307e" }
# embassy-usb ={ git = "https://github.com/embassy-rs/embassy", rev = "85606cc658695230c0d89ae6b815649ab9a9307e" }
# embassy-usb-driver ={ git = "https://github.com/embassy-rs/embassy", rev = "85606cc658695230c0d89ae6b815649ab9a9307e" }
# embassy-embedded-hal ={ git = "https://github.com/embassy-rs/embassy", rev = "85606cc658695230c0d89ae6b815649ab9a9307e" }